import { FormSignalError } from './form-signal-error';

describe('FormSignalError', () => {
  it('should create an instance', () => {
    const directive = new FormSignalError();
    expect(directive).toBeTruthy();
  });
});
