apiVersion: v1
kind: Service
metadata:
  name: {{ include "calendare-ui.fullname" . }}
  labels:
    {{- include "calendare-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "calendare-ui.selectorLabels" . | nindent 4 }}
